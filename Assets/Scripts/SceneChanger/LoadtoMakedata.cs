using System.Collections;
using System.Collections.Generic;
using UnityEngine;
//씬 너무 많이 나누나???
public class LoadtoMakedata : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
